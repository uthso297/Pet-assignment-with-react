import logo from '../../assets/images/logo.webp'

const Header = () => {
    return (
        <div>
            <div>
                <img src={logo} alt="" />
            </div>
        </div>
    );
};

export default Header;